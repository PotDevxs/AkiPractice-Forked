import java.nio.file.Paths

/*
 * This project can't be redistributed without
 * authorization of the developer
 *
 * Project @ AkiPractice
 * @author saki © 2026
 * Date: 11/02/2026
 */

plugins {
    id 'idea'
    id 'java'
    id 'java-library'
    id 'org.jetbrains.kotlin.jvm' version '1.9.10'
    id 'maven-publish'
    id 'com.github.johnrengelman.shadow' version '5.2.0'
}

group = 'aki.saki'
version = '1.0'

sourceCompatibility = 1.8
targetCompatibility = 1.8

repositories {
    mavenLocal()
    mavenCentral()
    maven { url 'https://repo.papermc.io/repository/maven-public/' }
    maven { url 'https://hub.spigotmc.org/nexus/content/repositories/snapshots/' }
    maven { url 'https://oss.sonatype.org/content/groups/public/' }
    maven { url 'https://maven.elmakers.com/repository/' }
    maven { url 'https://maven.enginehub.org/repo/' }
    maven { url 'https://repo.dmulloy2.net/repository/public/' }
    maven { url 'https://repo.codemc.io/repository/nms/' }
    maven { url 'https://jitpack.io' }
    maven { url 'https://repo.extendedclip.com/content/repositories/placeholderapi/' }
}

dependencies {
    implementation 'org.jetbrains.kotlin:kotlin-stdlib-jdk8:1.9.10'

    compileOnly 'org.spigotmc:spigot:1.8.8-R0.1-SNAPSHOT'
    compileOnly 'org.projectlombok:lombok:1.18.20'

    implementation 'com.github.jonahseguin:drink:master'
    implementation 'com.google.code.gson:gson:2.9.0'
    implementation 'org.mongodb:mongo-java-driver:3.12.10'
    implementation 'com.github.ThatKawaiiSam:Assemble:master'
    implementation 'com.github.cryptomorin:XSeries:7.9.1.1'

    compileOnly 'me.clip:placeholderapi:2.11.6'
    compileOnly 'com.github.ben-manes.caffeine:caffeine:2.9.3'
    compileOnly 'net.dmulloy2:ProtocolLib:5.1.0'
    implementation files('libs/FAWE.jar')
    // Ryu Core e Katto: o projeto inclui stubs em dev.ryu.core.bukkit e rip.katz.api para compilar sem os JARs.
    // Se tiver os JARs originais, coloque em libs/ryu-bukkit.jar e libs/katto.jar e remova os stubs em src/main/kotlin/dev e src/main/kotlin/rip.
    if (file('libs/samurai.jar').exists()) compileOnly files('libs/samurai.jar')

    compileOnly 'com.sk89q.worldedit:worldedit-core:6.0.0-SNAPSHOT'
    compileOnly 'com.sk89q.worldedit:worldedit-bukkit:6.0.0-SNAPSHOT'
}

compileJava.options.encoding = 'UTF-8'
tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}

tasks.withType(org.jetbrains.kotlin.gradle.tasks.KotlinCompile).configureEach {
    kotlinOptions {
        jvmTarget = "1.8"
        freeCompilerArgs += "-Xjsr305=strict"
    }
}

tasks.withType(ProcessResources) {
    filesMatching('**/*.properties') {
        filter { String line -> new String(line.getBytes('ISO-8859-1'), 'UTF-8') }
    }
    filesMatching('**/*.yml') {
        filter { String line -> new String(line.getBytes('ISO-8859-1'), 'UTF-8') }
    }
    filesMatching('**/*.json') {
        filter { String line -> new String(line.getBytes('ISO-8859-1'), 'UTF-8') }
    }
}

sourceSets {
    main.kotlin.srcDirs += 'src/main/kotlin'
    main.java.srcDirs += 'src/main/java'
}

// Ensure Java compiles after Kotlin so Java code can resolve Kotlin classes (e.g. PracticePlugin)
tasks.named('compileJava').configure { it.dependsOn(tasks.named('compileKotlin')) }

shadowJar {
    archiveClassifier.set('')
    project.configurations.implementation.canBeResolved = true
    configurations = [project.configurations.implementation]
}

// Ajuste para salvar na pasta build/libs dentro do projeto
tasks.withType(Jar).configureEach {
    archiveFileName = "${project.name}-${project.version}.jar"
}
